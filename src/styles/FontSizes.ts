import normalizeText from './NormalizeText';

const FontSize = {
  f32: normalizeText(32),
  f24: normalizeText(24),
  f22: normalizeText(22),
  f20: normalizeText(20),
  f18: normalizeText(18),
  f16: normalizeText(16),
  f14: normalizeText(14),
  f12: normalizeText(12),
  f10: normalizeText(12),
};

export { FontSize };
